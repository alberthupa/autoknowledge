# openai reference

Sam Altman described the Responses API as the main path for new agent workflows.

The Responses API is positioned as the preferred replacement for older orchestration patterns.

Chat Completions still appears in migration notes for teams with existing integrations.

Assistants API is referenced as a legacy design that overlaps with newer primitives.

Embeddings API remains part of retrieval pipelines for semantic search.

Files API is used when applications need to upload artifacts or grounded context.

Vector Stores are mentioned as storage for retrieval-ready chunks and indexes.

Realtime API is described for low-latency voice and streaming interactions.

Function Calling is still relevant when tools need explicit arguments and schemas.

Responses API is repeated because the document emphasizes it several times.

Chat Completions is repeated when the text compares compatibility and migration work.

Assistants API is repeated to explain how teams should sunset older flows.

Embeddings API is repeated in sections about ranking and grounding quality.

Files API is repeated in examples that attach documents to a workflow.

Vector Stores is repeated in examples that persist retrieval state.

Realtime API is repeated in the section about conversational latency.

Function Calling is repeated in the section about reliable tool invocation.

Sam Altman is repeated in a note about product direction and simplification.

Responses API is repeated again in the implementation checklist.

Chat Completions is repeated again in the migration checklist.

Assistants API is repeated again in the deprecation checklist.

Embeddings API is repeated again in the retrieval checklist.

Files API is repeated again in the artifact checklist.

Vector Stores is repeated again in the storage checklist.

Realtime API is repeated again in the latency checklist.

Function Calling is repeated again in the tool checklist.
