# Frozen Benchmarks

Frozen benchmarks are stable reference cases used for regression testing.

Current contents:

- `manifest.json` with file-ingestion regression cases
- success cases with minimum structural expectations
- a re-ingestion idempotence check
- an empty-input regression guard

These cases should change rarely and only with explicit review.
