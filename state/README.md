# State

This directory will store machine-readable runtime state, for example:

- experiment ledger
- last successful self-update run
- benchmark manifests
- scheduling checkpoints

Keep durable records here, but keep large generated reports under `artifacts/`.
